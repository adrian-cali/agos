# AGOS Backend Requirements
# Install with: pip install -r requirements.txt

# Web framework & ASGI server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# WebSocket support
websockets>=12.0

# Data validation (required by FastAPI)
pydantic>=2.5.0

# Environment variable management
python-dotenv>=1.0.0

# Firebase Admin SDK
firebase-admin>=6.4.0

# Redis client (optional — used for state caching when REDIS_URL env var is set)
# Falls back gracefully if not installed or REDIS_URL is not configured.
redis>=5.0.0
