# AGOS Backend Requirements
# Install with: pip install -r requirements.txt

# Web framework & ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# WebSocket support
websockets==12.0

# Data validation (required by FastAPI)
pydantic==2.5.3

# HTTP client (optional - for health checks / testing)
httpx==0.26.0

# Environment variable management (optional)
python-dotenv==1.0.0
